extends ../layouts/common.pug
block content 
    h1 Transfer
    
  
    form.form-group(action="/transfer/create", method="post")
            input(type="hidden", name="_csrf" value=csrfToken);
            label(for="account") Account
            input#account.form-control(type="text" , name ="accountId")
            label(for="amount") Password
            input#amount.form-control(type="number" , name ="amount" )
           
            button.mt-2.btn.btn-primary(type="submit") Tranfer